#ifndef CHIP8_EMULATOR_GPU_HPP
#define CHIP8_EMULATOR_GPU_HPP

/*
 * Original display resolution is 64x32 pixels, monochrome.
 * Drawing sprites, 8 pixels wide and 1-15 pixels height.
 * Sprite pixels are XOR'd with corresponding screen pixels.
 */

class GPU {
};

#endif // CHIP8_EMULATOR_GPU_HPP
