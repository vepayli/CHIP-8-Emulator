#ifndef CHIP8_EMULATOR_KEYBOARD_HPP
#define CHIP8_EMULATOR_KEYBOARD_HPP

/*
 * Input is done with hexkeyboard that has 16 keys ranged 0x0 - 0xF
 * the 0x8, 0x6, 0x4 and 0x2 keys are used as directional input.
 */

class Keyboard {
};

#endif // CHIP8_EMULATOR_KEYBOARD_HPP
